apply plugin: 'com.android.application'

android {
    compileSdkVersion 29
    buildToolsVersion "28.0.3"

    defaultConfig {
        applicationId "ccc.chess.engine.stockfish"
        minSdkVersion 14
        targetSdkVersion 29
        versionCode 26
        versionName "2.1"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

